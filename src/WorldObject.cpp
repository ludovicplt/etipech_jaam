#include "ICollidable.h"
#include "WorldObject.h"

namespace WorldObject {
    /*
    void WorldLoader::DisplayInfo(void)
    {
        for (int i = 0; i < map.size(); i++) {
        //    std::cout << "Name: " << _map[i].name << " Pos.x: " << _map[i].Pos.x << " Pos.y: " << _map[i].Pos.y << " Id: " << map[i].TextureId << std::endl;
        }
    }
    */
}